import React from "react";
import { WebView } from "react-native-webview";

const Privacy = () => {
  return <WebView source={{ uri: "https://bts-app.nodehome.io/privacy" }} />;
};

export default Privacy;
